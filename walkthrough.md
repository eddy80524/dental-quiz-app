# Performance and Stability Improvements Walkthrough

## 概要
ユーザーから報告された「頻繁な切断」と「動作の重さ」に対処するため、以下の3つの主要な改善を行いました。

## 1. データ読み込みの高速化 (Caching)
**課題:** ログインやリロードのたびにFirestoreから全学習履歴を読み込んでおり、データ量に比例して遅延が発生していた。
**対策:** `firestore_db.py` に `@st.cache_data` を適用した `cached_load_user_cards` 関数を追加しました。
- **効果:** 2回目以降の読み込み（リロード時など）がほぼ瞬時になります。キャッシュの有効期限は5分（300秒）です。
- **変更ファイル:** `my_llm_app/firestore_db.py`, `my_llm_app/app.py`

```python
@st.cache_data(ttl=300)
def cached_load_user_cards(uid: str) -> Dict[str, Any]:
    # Firestoreからデータを取得し、キャッシュする
    ...
```

## 2. セッション復帰の堅牢化 (Retry Logic)
**課題:** ネットワークの一時的な不調などでトークンリフレッシュが失敗すると、即座にログアウト扱いになっていた。
**対策:** `auth.py` の `refresh_token` メソッドにリトライロジック（最大3回）を追加しました。
- **効果:** 一時的なエラーでログアウトされる頻度が減少します。
- **変更ファイル:** `my_llm_app/auth.py`

```python
# 最大3回リトライ
max_retries = 3
for attempt in range(max_retries):
    try:
        # リフレッシュ試行
        ...
    except:
        # 待機してリトライ
        time.sleep(1 * (attempt + 1))
```

## 3. ランキング処理の負荷軽減
**課題:** 統計データが未作成の場合に実行される「リアルタイムランキング集計」が、全ユーザーデータをスキャンするため非常に重く、タイムアウトの原因になっていた。
**対策:** `firestore_db.py` の `fetch_ranking_data` におけるリアルタイム集計フォールバックを無効化しました。
- **効果:** サーバー負荷が下がり、他の処理への悪影響を防ぎます。統計データがない場合は空リストを返します。
- **変更ファイル:** `my_llm_app/firestore_db.py`

## 検証方法
1. **動作確認:** アプリを起動し、ログイン後の読み込み速度が改善されているか確認してください。特にリロード時の速度差が顕著なはずです。
2. **ログ確認:** コンソールログに `[INFO] ...件の学習データを読み込みました` が表示されるか、エラーが出ていないか確認してください。
3. **セッション維持:** アプリを放置して再操作した際、スムーズに復帰できるか確認してください。

## 次のステップ (Phase 2)
- Firestoreデータのページネーション対応
- 非同期書き込みの導入
- ランキング統計生成バッチの実装

## Phase 2: Review Limits & Search Tab Cleanup (今回実施)

### 1. 復習カード過多への対策 (Review Limits)
**課題:** 復習カードが大量に溜まった際、ユーザーが圧倒されてしまう問題がありました。
**対策:** `practice_page.py` に以下の制限機能を追加しました。
- **1日の復習上限 (Daily Review Limit):** 1日に出題される復習カードの上限を設定可能にしました（デフォルト120枚）。
- **1セットの問題数 (Cards Per Session):** 1回の学習セッションで出題される問題数を設定可能にしました（デフォルト10問）。
- **UI:** 練習ページのサイドバーに設定スライダーを追加しました。
- **変更ファイル:** `my_llm_app/modules/practice_page.py`

### 2. 検索タブの最適化 (Search Tab Cleanup)
**課題:** 検索タブの「Review Optimization」機能が重く、かつユーザーにとって不要な機能でした。
**対策:** `search_page.py` から以下の機能を削除しました。
- `build_balanced_review_plan` 関数および関連ロジックの削除
- "Daily Review Planner" UIセクションの削除
- 関連するセッション状態変数の削除
- **効果:** 検索タブの表示速度が向上し、コードベースが軽量化されました。
- **変更ファイル:** `my_llm_app/modules/search_page.py`

### 3. ローカルテスト環境の整備
**課題:** ローカルでFirebase連携をテストするための設定ファイルがありませんでした。
**対策:** `.streamlit/secrets.toml.example` を作成しました。
- ユーザーはこのファイルを `.streamlit/secrets.toml` にコピーし、Firebase認証情報を入力することで、ローカル環境でも本番同様のテストが可能になります。

### 4. 自由演習のカテゴリ選択機能 (Category Selection)
**課題:** 回数別モードで、必修・一般・臨床実地といった問題タイプごとの絞り込みができませんでした。
**対策:** `practice_page.py` にカテゴリ選択機能を追加しました。
- **UI:** 「回数別」モードの「領域」選択の下に「カテゴリ」選択ボックスを追加（全て/必修/一般/臨床実地）。
- **ロジック:** 選択されたカテゴリに基づき、問題番号から問題タイプを判定してフィルタリングするロジックを実装しました。
    - 必修: 1-20番
    - 一般: 21-65番
    - 臨床実地: 66-90番
- **変更ファイル:** `my_llm_app/modules/practice_page.py`

### 5. 学習メモ機能の改善 (Study Notes)
**課題:** メモ機能のUIが微妙で、アップロードした画像を後から一覧で見ることができませんでした。
**対策:**
- **UI改善:** メモと画像をドキュメント風に表示するように `NotesManager` を更新しました。
- **一覧ページ:** 全てのメモを閲覧・検索できる「学習メモ」ページを新設しました。
- **UI整理:** 検索・進捗ページから重複していた「メモ一覧」タブを削除しました。
- **変更ファイル:**
    - `my_llm_app/modules/notes_manager.py`: バケット名の修正、UI改善
    - `my_llm_app/modules/notes_page.py`: 新規作成（メモ一覧ページ）
    - `my_llm_app/app.py`: ナビゲーションに「学習メモ」を追加
    - `my_llm_app/modules/search_page.py`: メモ一覧タブの削除

### 6. グラフ分析の刷新 (Subject Performance Matrix)
**課題:** 科目別の進捗率と正答率が別々のグラフで表示されており、弱点科目の発見が困難でした。
**対策:** 進捗率×正答率の散布図（バブルチャート）に統合しました。
- **可視化:** X軸=進捗率、Y軸=正答率、バブルサイズ=問題数
- **4象限分析:**
    - 右上（高進捗×高正答率）: ✅ マスター
    - 左上（低進捗×高正答率）: 📈 ポテンシャル
    - **右下（高進捗×低正答率）: ⚠️ 要復習（最優先）**
    - 左下（低進捗×低正答率）: 🆕 未着手
- **補助線:** 進捗50%、正答率80%（合格ライン）
- **象限別リスト:** グラフの下に各象限の科目を自動分類
- **変更ファイル:** `my_llm_app/modules/search_page.py`

## 検証手順 (Phase 2)
1. **復習制限の確認:**
   - 練習ページのサイドバーにある「1日の復習上限」と「1セットの問題数」スライダーを変更し、学習セッション（おまかせ学習）を開始して、出題数が設定通りになるか確認してください。
2. **検索タブの確認:**
   - 検索タブを開き、動作が軽快になっているか、不要な「Review Planner」が表示されていないか確認してください。
3. **ローカル実行:**
   - `.streamlit/secrets.toml` を設定し、`streamlit run my_llm_app/app.py` でアプリを起動して、Firebase連携が正常に動作するか確認してください。
4. **カテゴリ選択の確認:**
   - 自由演習モードで「回数別」を選択し、「カテゴリ」で「必修」などを選んで開始してください。出題される問題が指定したカテゴリ（番号範囲）のみになっているか確認してください。
5. **学習メモの確認:**
   - 演習中にメモと画像を追加し、保存できるか確認してください。
   - サイドバーから「学習メモ」ページに移動し、保存したメモが表示されるか確認してください。
